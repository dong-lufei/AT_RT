## 后端调整
1. 删除受保护路由 `/protected`
   - 位置：`f:\Desktop\tk\index.js:163-166`
   - 同时移除启动时接口收集中的该项（自动随路由移除）
2. 重命名并保护“获取所有用户”接口
   - 现有位置：`f:\Desktop\tk\index.js:169-179` 的 `router.get('/users', ...)`
   - 变更为：`router.get('/users-all', authenticateToken, ...)`
   - 行为：始终返回全部用户数组（不再包裹为 `{users: [...]}`），响应结构为 `{code:200, msg:'OK', data:[...]}`
   - 认证：必须携带 `Authorization: Bearer <accessToken>`
3. 保持统一响应结构
   - 顶层顺序 `{code, msg, data}`，无数据时 `data: {}`，成功 `code=200`，错误为对应 HTTP 状态码
4. 启动日志保持：方法名彩色、URL 整体着色（服务器地址及接口表格），以便点击访问

## 前端测试页 `test.html` 调整
1. 删除 `/protected` 相关的整个测试区块（按钮、结果展示）
2. 新增“获取所有用户（受保护）”测试区块
   - 请求：`GET {BASE_PATH}/users-all`
   - 自动携带 `Authorization: Bearer <accessToken>`（登录成功后自动写入）
   - 渲染：显示接口返回的 `{code, msg, data}`，其中 `data` 为数组
3. 输入框默认值与自动填充
   - 默认填充：`BASE_URL='http://localhost:3000'`、`BASE_PATH='/api'`、`username='admin'`、`password='password123'`
   - 登录成功后：自动将 `accessToken` 写入 `accessInput`，`refreshToken` 写入 `refreshInput`
   - 保证“当前文本”和“accessToken”所在行不换行：为这两行添加 `white-space: nowrap` 样式（例如给行加类名 `.nowrap`）
4. URL 拼接
   - 继续使用 `joinUrl(buildBase(), path)`，避免出现 `//login` 等双斜杠
5. 展示与换行
   - `pre` 继续对 JSON 自动换行以适应宽度
   - 仅对“当前文本”和“accessToken”行应用不换行样式

## 文档 `README.md` 更新
1. 移除 `/protected` 相关说明
2. 更新接口列表为：
   - `POST /api/login`
   - `POST /api/refresh`
   - `GET /api/users-all`（受保护）
3. 保持示例响应结构为 `{code, msg, data}`，`data` 在无数据时为 `{}`

## 验证与运行
- 使用 `npm run dev`（已监听 `.env` 和源文件）进行热更新验证
- 启动后在终端确认：服务器地址和彩色接口列表；不存在 `/protected`；存在 `/users-all`
- 在 `test.html` 测试：
  - 登录 → 自动写入 token；
  - 刷新 → `accessToken` 更新；
  - 获取所有用户（受保护） → 返回数组并正确渲染到 `pre` 中；
  - 检查“当前文本”和“accessToken”行不换行